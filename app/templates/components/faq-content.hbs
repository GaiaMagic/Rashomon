<div {{bind-attr id=for}} class="section-content u-pullRight">
    <h2 class="section-header">{{header}}</h2>

    {{#each faqList}}
        <div class="faq-item u-paddingBm u-marginBl">
            <div class="question">
                <span>Q: </span>
                {{question}}？
            </div>

            <div class="answer">
                <span>A: </span>

                <span>{{answer}}</span>

                {{#if image}}
                    <img {{bind-attr src=image alt="image"}} class="u-marginTm u-marginBm" />
                {{/if}}
            </div>
        </div>
    {{/each}}
</div>
